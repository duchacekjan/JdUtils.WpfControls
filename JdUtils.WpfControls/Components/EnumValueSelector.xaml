<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:JdUtils.WpfControls.Components"
                    xmlns:cnv="jgs:converters">
    <Style TargetType="{x:Type local:EnumValueSelector}">
        <Setter Property="ItemsSource" Value="{Binding RelativeSource={RelativeSource Self}, Path=EnumType, Converter={cnv:EnumValuesConverter}}"/>
        <Setter Property="DisplayMemberPath" Value="Value"/>
        <Setter Property="SelectedValuePath" Value="Key"/>
    </Style>
</ResourceDictionary>